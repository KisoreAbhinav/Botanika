# Configuration boundary

Versioned, non-secret runtime defaults and model contracts will live here.
Deployment secrets, tunnel credentials, API keys, and machine-local overrides
must stay outside Git.
