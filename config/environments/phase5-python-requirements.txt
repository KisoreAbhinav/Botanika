# Botanika Phase 5/6 web-application direct Python runtime pins.
#
# Phase 5 introduces the FastAPI modular monolith and Phase 6 adds the local
# catalog/library routes. These pins
# were resolved on Raspberry Pi OS (Debian trixie) with Python 3.13.
# python-multipart is required for the local-image fallback upload endpoint.
# HTTPX2 drives ASGI contract tests; AnyIO is pinned for repeatable async behavior.
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
httpx2==2.12.0
anyio==4.14.2
playwright==1.61.0
